{
  "runtime": {
    "noProvider": "No provider could be resolved.\n  Run `ghost init` to configure one interactively, pass --provider <id> --model <model>,\n  export the provider's API key variable, or set agents.defaults in {{configFile}}.\n  Known providers: {{ids}}",
    "noModel": "No model configured for {{provider}}.\n  Run `ghost init`, pass --model <model>, or set agents.defaults.model in {{configFile}}."
  },
  "server": {
    "noModel": "No model is configured. Add a provider and choose a model in Settings, or run `ghost init` from a terminal.",
    "authDisabled": "Authentication is disabled on this server",
    "badCredentials": "Incorrect username or password",
    "alreadyClaimed": "This server already has a password; sign in instead",
    "badSetupCode": "Incorrect or already-used setup code",
    "currentPasswordRequired": "The current password is required to change it",
    "currentPasswordWrong": "Incorrect current password",
    "authRequired": "Authentication required",
    "sessionInvalid": "Invalid or expired session",
    "signedUrlRequired": "A signed URL is required",
    "signedUrlInvalid": "Invalid or expired signed URL",
    "internal": "Internal server error"
  }
}
