{
  "name": "@ghostai/protocol",
  "version": "0.0.0",
  "description": "Zod schemas and derived types shared by every GhostAI package.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.test.*"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
