/**
 * Settings — Step 18.
 */

import type { JSX } from 'react';

import { Placeholder } from './placeholder.js';

export function SettingsRoute(): JSX.Element {
  return (
    <Placeholder title="Settings" step="Step 18">
      Agent, providers with vault-backed key entry, the tool approval matrix and the context
      inspector.
    </Placeholder>
  );
}
